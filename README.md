# 2048 — Java 课程设计

> **华南理工大学 · JAVA 程序设计课程大作业**
> Author: Liang Pang | Student ID: 202030100266

---

## 项目背景

近年来，移动手游成为电子游戏产业的重要一环。作为手游史上的经典之作，**2048** 是一款兼具趣味性与益智性的开山鼻祖级游戏。

本项目基于 Java Swing 实现完整的 2048 游戏，涵盖图形界面、键盘交互、计分、存档读档、音效和 AI 智能提示等功能。

## 游戏规则

在 4×4 的 16 宫格中，使用 **↑ ↓ ← →** 方向键操作：

- 所有方块向指定方向滑动
- 相邻且相同的数字合并为它们的和
- 每次操作后在随机空格生成 2（90%）或 4（10%）
- 当 16 宫格填满且四个方向都无法合并时，游戏结束
- 目标是合并出 **2048** 并获得尽可能高的分数

## 快捷键

| 按键 | 功能 |
|------|------|
| ↑ ↓ ← → | 方向移动 |
| Esc | 重新开始 |
| M | 开关音效 |
| S | 存档 |
| L | 读档 |
| T | AI 智能提示 |

## 项目结构

```
JAVA2048/
├── README.md
├── src/
│   ├── Main.java          # 程序入口 (EDT 启动)
│   ├── Grid.java           # 格子类 — 数值、颜色、字体、递归移动
│   ├── GameView.java       # 主界面 + GameBoard 内部类
│   ├── PlaySound.java      # 音效播放线程
│   └── Utils.java          # XML 存档 + AI 提示 (Expectimax)
├── tests/
│   ├── TestAll.java        # 测试套件入口 (汇总全部测试)
│   ├── GridTest.java       # Grid 单元测试 (15 用例)
│   ├── GameLogicTest.java  # 游戏逻辑测试 (10 用例)
│   └── UtilsTest.java      # Utils 工具测试 (5 用例)
├── data/
│   └── test_save.xml       # 测试用存档样例
├── res/
│   ├── move.wav            # 移动音效 (可选)
│   └── merge.wav           # 合并音效 (可选)
├── out/                    # 编译输出目录 (自动生成)
└── save.xml                # 游戏存档 (运行时生成)
```

## 系统设计

```
┌──────────────────────────────┐
│          GameView             │
│  ┌────────────────────────┐  │
│  │     标题 & 分数区       │  │
│  ├────────────────────────┤  │
│  │      GameBoard         │  │
│  │   ┌──┬──┬──┬──┐       │  │
│  │   │  │  │  │  │       │  │
│  │   ├──┼──┼──┼──┤       │  │
│  │   │  │  │  │  │       │  │
│  │   ├──┼──┼──┼──┤       │  │
│  │   │  │  │  │  │       │  │
│  │   ├──┼──┼──┼──┤       │  │
│  │   │  │  │  │  │       │  │
│  │   └──┴──┴──┴──┘       │  │
│  └────────────────────────┘  │
│  ┌────────────────────────┐  │
│  │    按键提示 & 菜单栏    │  │
│  └────────────────────────┘  │
└──────────────────────────────┘
```

### 类设计

| 类 | 职责 |
|----|------|
| `Main` | 程序入口，在 EDT 线程启动 GUI |
| `Grid` | 单个格子的数据与行为：数值、配色、字体、四方向递归移动 |
| `GameView` | 顶层窗口：布局、菜单、分数显示、事件监听 |
| `GameView.GameBoard` | 游戏面板：棋盘状态、移动合并、胜负判定、绘制、键盘响应、存档 |
| `PlaySound` | 异步线程播放 WAV 音效 |
| `Utils` | 工具函数：XML 读写、AI Expectimax 搜索 |

## 核心设计

### 递归移动算法

摒弃传统的迭代方式，采用**递归**实现方块移动：

1. 从移动方向远端开始遍历每个非空格子
2. 检查相邻格子：若为空则交换并递归推进；若值相同且未合并则合并
3. `merge` 标志防止同一次操作中的连环合并

```java
// Grid.java — 以向上移动为例
public int moveUp(Grid[][] grids, int i, int j) {
    if (i == 0) return 0;           // 递归基
    Grid prev = grids[i - 1][j];
    if (prev.isEmpty()) {            // 空格 → 滑入并递归
        prev.value = this.value;
        this.value = 0;
        return prev.moveUp(grids, i - 1, j);
    } else if (prev.value == this.value && !prev.merge) {
        prev.merge = true;           // 合并
        prev.value *= 2;
        this.value = 0;
        return prev.value;
    }
    return 0;
}
```

### AI 智能提示 — 2-ply Expectimax

相比原版单步贪心搜索，优化后的提示使用 **两层 Expectimax** 算法：

```
对每个可行方向 dir:
  1. 在副本上模拟移动，得到 immediate_score
  2. 枚举所有空格:
     - 填入 2 (权重 0.9):  继续模拟四方向，取 max_score
     - 填入 4 (权重 0.1):  继续模拟四方向，取 max_score
  3. expected_score = immediate_score + Σ(weight × max_score)
  4. 选择 expected_score 最高的方向
```

这更贴近 2048 的真实博弈结构 —— 玩家（移动）对抗随机环境（落子）。

### 数字配色方案

遵循"色相统一、色调渐变、突出强调"原则：

| 数值 | 背景色 | 前景色 |
|------|--------|--------|
| 0 | `#cdc1b4` | — (隐藏) |
| 2 | `#eee4da` | 黑色 |
| 4 | `#ede0c8` | 黑色 |
| 8 | `#f2b179` | 白色 |
| 16 | `#f59563` | 白色 |
| 32 | `#f67c5f` | 白色 |
| 64 | `#f65e3b` | 白色 |
| 128 | `#edcf72` | 白色 |
| 256 | `#edcc61` | 白色 |
| 512 | `#edc850` | 白色 |
| 1024 | `#edc53f` | 白色 |
| 2048 | `#edc22e` | 白色 |
| ≥4096 | `#248c51` | 白色 |

---

## 环境准备

### 系统要求

- **JDK 8 或更高版本** (编译需要 `javac`，运行仅需 `java`)
- 无外部依赖 —— 仅使用 Java 标准库 (`javax.swing`, `javax.sound`, `java.io` 等)
- 操作系统: Windows / Linux / macOS 均可

### 检查 Java 环境

```bash
java -version    # 应显示 1.8.0 或更高
javac -version   # 确认 JDK 已安装 (非 JRE)
```

如果提示 `javac: 未找到命令`，说明当前只安装了 JRE (运行环境)，缺少 JDK (开发环境):

```bash
# Ubuntu / Debian
sudo apt-get install openjdk-8-jdk

# CentOS / RHEL
sudo yum install java-1.8.0-openjdk-devel

# macOS
# 从 https://adoptium.net 下载安装
```

---

## 编译与运行游戏

### 第一步: 编译源代码

打开终端，进入项目根目录:

```bash
cd /home/kemove/Courses/JAVA2048

# 创建编译输出目录
mkdir -p out

# 编译所有源文件 (UTF-8 编码)
javac -encoding UTF-8 -d out src/*.java
```

**参数说明:**
| 参数 | 含义 |
|------|------|
| `-encoding UTF-8` | 指定源文件编码，确保中文注释正确编译 |
| `-d out` | 编译产物输出到 `out/` 目录 |
| `src/*.java` | 编译 `src/` 下所有 `.java` 文件 |

编译成功后，`out/` 目录下会出现 `.class` 文件:

```
out/
├── Main.class
├── Grid.class
├── GameView.class
├── GameView$GameBoard.class    # 内部类
├── PlaySound.class
└── Utils.class
```

### 第二步: 运行游戏

```bash
java -cp out Main
```

游戏窗口将居中弹出。可使用 ↑↓←→ 方向键操作，Esc 重新开始。

### 第三步: 配置音效 (可选)

将 WAV 格式的音效文件放入 `res/` 目录:

```
res/
├── move.wav      # 方块移动音效
└── merge.wav     # 方块合并音效
```

无音效文件时游戏正常运行，音效静默跳过，不会报错。

---

## 测试

### 测试的目的

> **回归测试 (Regression Testing)**: 当对源代码进行任何修改（重构、优化、添加新功能、修复 bug）后，运行测试套件可以快速验证**已有的功能是否仍然正确**。如果某个测试从 "通过" 变为 "失败"，说明你的修改引入了新问题 —— 这样可以尽早发现并修复，避免将 bug 带入最终版本。

测试覆盖的三大核心模块:

| 模块 | 文件 | 测试重点 | 用例数 |
|------|------|----------|--------|
| **Grid** | `GridTest.java` | 构造、拷贝、颜色映射、字体大小、merge 标志、四方向移动、递归链、阻塞 | 15 |
| **GameLogic** | `GameLogicTest.java` | 空格检测、方块生成、计分累加、胜利判定、GameOver 三种场景、XML 存档解析 | 10 |
| **Utils** | `UtilsTest.java` | XML 写入/读取完整回合、AI 提示返回合法方向、深拷贝独立性 | 5 |

### 编译测试代码

测试代码位于 `tests/` 目录，需要与 `src/` 下的源码一起编译:

```bash
cd /home/kemove/Courses/JAVA2048

# 将 src 加入 classpath 再编译，这样测试代码可以直接引用 Grid、Utils 等类
javac -encoding UTF-8 -cp src -d out src/*.java tests/*.java
```

**参数说明:**
| 参数 | 含义 |
|------|------|
| `-cp src` | 将 `src/` 加入编译 classpath，使测试代码能 import Grid/Utils 等类 |
| `-d out` | 所有 `.class` 文件输出到同一 `out/` 目录 |
| `src/*.java tests/*.java` | 同时编译源码和测试代码 |

编译后 `out/` 目录新增测试类:

```
out/
├── ... (原有类)
├── TestAll.class
├── GridTest.class
├── GameLogicTest.class
└── UtilsTest.class
```

### 运行全部测试 (推荐)

```bash
java -cp out TestAll
```

**预期输出示例:**

```
╔══════════════════════════════════╗
║      2048 Game - Test Suite      ║
╚══════════════════════════════════╝

━━━ Grid.java 单元测试 ━━━

========================================
  Grid 单元测试
========================================

[构造]
  ✓ default value = 0
  ✓ value 2 constructor
  ✓ value 2048 constructor
[深拷贝]
  ✓ copy same value
  ✓ original unchanged after copy mutation
  ... (省略中间输出)
========================================
  Grid: 27 passed, 0 failed
========================================

━━━ 游戏逻辑测试 ━━━
  ... (省略)
  GameLogic: 11 passed, 0 failed

━━━ Utils 工具类测试 ━━━
  ... (省略)
  Utils: 11 passed, 0 failed

╔══════════════════════════════════╗
║  ALL 49 TESTS PASSED             ║
╚══════════════════════════════════╝
```

- **退出码 0** = 全部测试通过
- **退出码 1** = 存在测试失败（定位到具体失败的用例修改即可）

### 单独运行某个测试模块

修改代码后如果只想验证受影响的部分:

```bash
# 只测试 Grid 类
java -cp out GridTest

# 只测试游戏逻辑
java -cp out GameLogicTest

# 只测试工具类
java -cp out UtilsTest
```

### 测试失败时的排查流程

1. 查看输出中标记 `✗` 的行，确认是哪个测试用例失败
2. 对比失败用例的描述与你最近的修改，定位可能的原因
3. 修复后重新运行该模块确认通过
4. 最后运行 `TestAll` 确认没有引入新的连带问题

### 典型开发工作流

```
修改代码 → 编译 → 运行测试 → 全部通过？→ 运行游戏手工验证
                ↓ 失败
          检查失败用例 → 修复 → 重新编译测试
```

这个循环确保每次改动都是安全的、可追溯的。

---

## 已实现功能

### 核心玩法
- [x] 4×4 棋盘，随机生成 2/4
- [x] 四方向递归移动与合并
- [x] merge 标志防止连环合并
- [x] 实时计分与最高分记录
- [x] 游戏结束判定
- [x] 达成 2048 胜利提示

### 辅助功能
- [x] 存档/读档（XML 格式）
- [x] 音效开关（移动 + 合并）
- [x] AI 智能提示（2-ply Expectimax）
- [x] 退出确认对话框
- [x] Game 菜单（规则/关于/退出）

### UI/UX
- [x] 经典暖色系配色
- [x] 数字自适应字体大小
- [x] 抗锯齿渲染
- [x] Game Over / Victory 半透明遮罩
- [x] 实时音效状态指示
- [x] 底部按键提示栏

---

## 可拓展方向

1. **动画效果** — 数字产生与合并的过渡动画
2. **更强 AI** — 4-ply Expectimax + 启发式评估函数（空位数、单调性、平滑度）
3. **深度学习** — 基于强化学习训练智能通关助手
4. **联网排行榜** — 分数上传与全球排名
5. **多尺寸棋盘** — 支持 3×3 / 5×5 / 6×6

## 设计心得

本次课程设计的核心是基于 Java Swing 开发经典游戏 2048。在实现过程中遇到了连环合并、数字覆盖、分数累加遗漏等问题，需要对比运行结果与预期行为，经过仔细分析才能定位错误。

关键设计决策：

- **递归代替迭代**：使代码更简洁，逻辑更清晰，天然避免了一次操作中的连环合并问题。
- **解耦移动与合并**：整体定向移动由 GameBoard 负责，单个格子的移动合并由 Grid 实现，提高了代码复用性。
- **深拷贝隔离 AI 搜索**：AI 模拟必须在独立副本上进行，避免污染当前游戏状态。

通过本次课程设计，不仅锻炼了问题划分与求解、算法设计与分析的能力，更在实践中加深了对 Java 面向对象编程思想的理解。

## 参考文献

1. [美] 埃尔克. *Java 核心技术*. 机械工业出版社, 2012
2. [美] 埃尔克. *Java 编程思想*. 机械工业出版社, 2007
3. [美] 科尔曼. *算法导论(第三版)*. 机械工业出版社, 2012
4. [美] 罗素. *人工智能(现代方法)*. 人民邮电出版社, 2022
5. 刘彦君, 金飞虎. *JavaEE 开发技术与案例教程*. 人民邮电出版社, 2014
